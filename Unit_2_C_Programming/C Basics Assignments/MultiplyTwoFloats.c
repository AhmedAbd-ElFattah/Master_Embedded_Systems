#include <stdio.h>

int main(){
  float num_1, num_2, muliplied_val;
  printf("Enter Two integers: ");
  scanf("%f",&num_1);
  scanf("%f",&num_2);
  muliplied_val = num_1 * num_2;
  printf("Product : %f \n", muliplied_val);
  return 0;
}

